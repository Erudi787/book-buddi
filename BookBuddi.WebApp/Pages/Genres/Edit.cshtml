@page
@model BookBuddi.Pages.Genres.EditModel
@{
    ViewData["Title"] = "Edit Genre";
    var isAdmin = HttpContext.Session.GetString("UserRole") == "Admin";
}

<h1>Edit Genre</h1>

@if (!isAdmin)
{
    <p style="color: red;">Admin access required.</p>
    <p><a href="/Account/Login">Login as Admin</a></p>
}
else
{
    <form method="post" style="max-width: 600px;">
        <input type="hidden" asp-for="Genre.GenreId" />
        <input type="hidden" asp-for="Genre.CreatedBy" />
        <input type="hidden" asp-for="Genre.CreatedTime" />

        <div style="margin-bottom: 15px;">
            <label style="font-weight: bold;">Genre Name: *</label><br/>
            <input type="text" asp-for="Genre.GenreName" required style="width: 100%; padding: 8px;" maxlength="100" />
        </div>
        <div style="margin-bottom: 15px;">
            <label style="font-weight: bold;">Description:</label><br/>
            <textarea asp-for="Genre.GenreDescription" rows="4" style="width: 100%; padding: 8px;" maxlength="500"></textarea>
        </div>
        <div style="margin-bottom: 15px;">
            <p style="color: #666; font-size: 14px;">
                Created by: <strong>@Model.Genre.CreatedBy</strong> on @Model.Genre.CreatedTime.ToString("MMM dd, yyyy hh:mm tt")
            </p>
        </div>
        <div>
            <button type="submit" style="padding: 10px 20px; background-color: #007bff; color: white; border: none; border-radius: 5px; cursor: pointer;">Update Genre</button>
            <a href="/Genres/Index" style="margin-left: 10px; padding: 10px 20px; background-color: #6c757d; color: white; text-decoration: none; border-radius: 5px;">Cancel</a>
        </div>
    </form>

    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
    {
        <div style="margin-top: 20px; padding: 10px; background-color: #f8d7da; color: #721c24; border: 1px solid #f5c6cb; border-radius: 5px;">
            @Model.ErrorMessage
        </div>
    }
}
