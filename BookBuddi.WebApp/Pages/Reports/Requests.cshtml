@page
@model BookBuddi.WebApp.Pages.RequestsModel
@using BookBuddi.Data.Models
@{
    ViewData["Title"] = "Request Report";
}

<div class="report-container">
    <div class="report-header">
        <h1 class="report-title">Book Report</h1>
        
        <!-- Tab Navigation -->
        <div class="tab-navigation">
            <a href="/Reports/Borrowing" class="tab">Borrowing</a>
            <a href="/Reports/Inventory" class="tab">Inventory</a>
            <a href="/Reports/Members" class="tab">Members</a>
            <a href="/Reports/Requests" class="tab active">Request</a>
            <a href="/Reports/LostBooks" class="tab">Lost Book</a>
        </div>
    </div>

    <!-- Summary Cards -->
    <div class="summary-grid">
        <div class="summary-card total-requests">
            <div class="card-content">
                <h2>@Model.TotalRequests</h2>
                <p>Total Request</p>
            </div>
        </div>

        <div class="summary-card approved-requests">
            <div class="card-content">
                <h2>@Model.ApprovedRequests</h2>
                <p>Approved</p>
            </div>
        </div>

        <div class="summary-card pending-requests">
            <div class="card-content">
                <h2>@Model.RejectedRequests</h2>
                <p>Denied</p>
            </div>
        </div>

        <div class="summary-card rejected-requests">
            <div class="card-content">
                <h2>@Model.PendingRequests</h2>
                <p>Pending</p>
            </div>
        </div>
    </div>

    <!-- Controls -->
    <div class="controls-bar">
        <div class="search-box">
            <input type="text" placeholder="Search for name or E-mail" />
        </div>
        
        <div class="filter-controls">
            <select class="filter-dropdown">
                <option>Status</option>
            </select>
            <select class="filter-dropdown">
                <option>Sort date</option>
            </select>
        </div>

        <div class="action-buttons">
            <button class="btn-export btn-export-pdf">Export PDF</button>
            <button class="btn-export btn-export-csv">Export CSV</button>
        </div>
    </div>

    <!-- Requests Table -->
    <div class="table-container">
        <table class="report-table">
            <thead>
                <tr>
                    <th>Book Title Requested</th>
                    <th>Name</th>
                    <th>Request Date</th>
                    <th>Status</th>
                    <th>Action</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var r in Model.RequestList)
                {
                    <tr>
                        <td>@r.BookTitle</td>
                        <td>@r.MemberName</td>
                        <td>@r.RequestDate.ToString("MMM dd, yyyy")</td>
                        <td>
                            <span class="status @r.StatusClass">@r.StatusText</span>
                        </td>
                        <td>
                            <div class="action-icons">
                                <button class="icon-btn approve-btn" title="Approve">✓</button>
                                <button class="icon-btn delete-btn" title="Delete">×</button>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<style>
    .report-container {
        padding: 0;
        background: #f5f6fa;
        min-height: 100vh;
    }

    .report-header {
        padding: 30px;
        background: white;
        border-bottom: 1px solid #e0e0e0;
    }

    .report-title {
        font-size: 32px;
        margin: 0 0 20px 0;
        font-weight: bold;
        color: #2c3e50;
    }

    /* Tab Navigation */
    .tab-navigation {
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
    }

    .tab {
        padding: 10px 20px;
        background: #f0f0f0;
        border: none;
        border-radius: 20px;
        color: #666;
        text-decoration: none;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.3s;
    }

    .tab:hover {
        background: #e0e0e0;
    }

    .tab.active {
        background: #8b3a3a;
        color: white;
    }

    /* Summary Grid */
    .summary-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 20px;
        padding: 30px;
        background: white;
        border-bottom: 1px solid #e0e0e0;
    }

    .summary-card {
        padding: 20px;
        background: #f9f9f9;
        border-radius: 8px;
        border-left: 4px solid #8b3a3a;
    }

    .card-content {
        text-align: center;
    }

    .summary-card h2 {
        font-size: 36px;
        margin: 0;
        font-weight: bold;
        color: #2c3e50;
    }

    .summary-card p {
        font-size: 14px;
        margin: 8px 0 0 0;
        color: #999;
    }

    /* Controls Bar */
    .controls-bar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 20px;
        padding: 20px 30px;
        background: white;
        border-bottom: 1px solid #e0e0e0;
        flex-wrap: wrap;
    }

    .search-box input {
        width: 300px;
        padding: 10px 15px;
        border: 1px solid #ddd;
        border-radius: 6px;
        font-size: 14px;
    }

    .filter-controls {
        display: flex;
        gap: 10px;
    }

    .filter-dropdown {
        padding: 10px 15px;
        border: 1px solid #ddd;
        border-radius: 6px;
        background: white;
        cursor: pointer;
        font-size: 14px;
    }

    .action-buttons {
        display: flex;
        gap: 10px;
    }

    .btn-export {
        padding: 10px 20px;
        border: none;
        border-radius: 6px;
        font-weight: 600;
        cursor: pointer;
        font-size: 14px;
        transition: all 0.3s;
    }

    .btn-export-pdf {
        background: #8b3a3a;
        color: white;
    }

    .btn-export-pdf:hover {
        background: #6d2e2e;
    }

    .btn-export-csv {
        background: white;
        color: #666;
        border: 1px solid #ddd;
    }

    .btn-export-csv:hover {
        background: #f5f5f5;
    }

    /* Table */
    .table-container {
        padding: 30px;
        background: white;
        overflow-x: auto;
    }

    .report-table {
        width: 100%;
        border-collapse: collapse;
        font-size: 14px;
    }

    .report-table th {
        background: #f5f5f5;
        padding: 15px;
        text-align: left;
        font-weight: 600;
        color: #2c3e50;
        border-bottom: 2px solid #e0e0e0;
    }

    .report-table td {
        padding: 15px;
        border-bottom: 1px solid #e0e0e0;
        color: #555;
    }

    .report-table tr:hover {
        background: #f9f9f9;
    }

    /* Status Badge */
    .status {
        display: inline-block;
        padding: 6px 12px;
        border-radius: 4px;
        font-size: 13px;
        font-weight: 600;
        color: white;
    }

    .status-pending {
        background: #f1c40f;
        color: #333;
    }

    .status-approved {
        background: #2ecc71;
    }

    .status-rejected {
        background: #e74c3c;
    }

    /* Action Buttons */
    .action-icons {
        display: flex;
        gap: 8px;
    }

    .icon-btn {
        width: 32px;
        height: 32px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-size: 18px;
        font-weight: bold;
        transition: all 0.3s;
    }

    .approve-btn {
        background: #2ecc71;
        color: white;
    }

    .approve-btn:hover {
        background: #27ae60;
    }

    .delete-btn {
        background: #e0e0e0;
        color: #999;
    }

    .delete-btn:hover {
        background: #d0d0d0;
    }
</style>